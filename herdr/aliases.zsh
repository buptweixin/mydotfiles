# herdr — persistent terminal runtime for coding agents.
# Config: herdr/config.toml (linked to ~/.config/herdr/config.toml).
# Meant to run standalone in a ghostty tab/window: cmd+t, then `hr`.
# Prefix is C-a (see herdr/config.toml), matching tmux muscle memory.
alias hr='exec herdr'
